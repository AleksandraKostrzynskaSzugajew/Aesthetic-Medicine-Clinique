spring:
  datasource:
    url: jdbc:mysql://localhost:3306/kme
    username: root
    password: coderslab
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
  mvc:
    format:
      time: HH:mm:ss
    view:
      prefix: /WEB-INF/views/
      suffix: .jsp
    static-path-pattern: /**
    resources:
      chain:
        strategy:
          content:
            mime-types:
              js: application/javascript
  security:
    user:
      name: olala
      password: prosiaczek
  sql:
    init:
      data-locations: classpath:import.sql
  web:
    resources:
      chain:
        strategy:
          content:
            enabled: true

